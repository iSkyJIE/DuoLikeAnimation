# DuoLikeAnimation Android port

Android 13+ port of the iOS SwiftUI/Metal demo. It uses RuntimeShader (AGSL) for the frosted-glass reprojection and Android rotation-vector sensors for the calibrated fold angle.

Build locally with `gradle assembleRelease` from this directory, or use the repository's `Build Android APK` GitHub Actions workflow on branch `android-apk`.
